import React from 'react';
import Typography from "@material-ui/core/Typography";

function StartingVoting(props) {
    return (
        <div>
            <Typography variant='h6'>Hinweis: Im nächsten Schritt werden Sie aufgefordert, Zugriff auf Ihre Kamera zu gewähren. Wählen Sie dort "Zulassen".</Typography>
        </div>
    );
}

export default StartingVoting;